# 生产环境默认走同域名反向代理，例如:
# https://your-domain.com -> 前端静态资源
# https://your-domain.com/api/v1 -> Spring Boot 后端
VITE_API_BASE_URL=http://localshot:8921/api/v1

# 如果前后端分域部署，可改成完整地址，例如:
# VITE_API_BASE_URL=https://api.your-domain.com/api/v1
